# Temporal Fall Detection Requirements for TianWan
# Install these additional packages for fall detection functionality

# Core ML libraries
torch>=1.9.0
torchvision>=0.10.0
numpy>=1.21.0

# Computer Vision
opencv-python>=4.5.0
ultralytics>=8.0.0  # For YOLOv8 pose estimation

# DirectML support for Windows GPU acceleration (optional)
torch-directml>=0.1.11.120.dev221125; sys_platform=="win32"

# Pose estimation and graph neural networks
# Note: These may need to be installed from the fall-detection submodule
# or manually installed based on the specific ST-GCN implementation

# Additional utilities
scipy>=1.7.0
scikit-learn>=1.0.0

# Logging (already included in main TianWan)
loguru>=0.6.0

# Flask (already included in main TianWan)  
flask>=2.0.0
